############################################################
# Runtime dependencies ONLY (used in production execution) #
############################################################
Flask>=3.0,<4
python-dotenv>=1.0.1
openai>=1.35.0
google-generativeai>=0.7.0
requests>=2.28.0

# Dev/test dependencies have moved to requirements-dev.txt
# Keep this file lean so `pip install -r requirements.txt` pulls
# only what the running app needs.
